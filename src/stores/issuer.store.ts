// src/stores/issuer.store.ts
import { create } from 'zustand';

interface IssuerState {
  // Add issuer state here
}

export const useIssuerStore = create<IssuerState>(() => ({
  // Initial state
}));
