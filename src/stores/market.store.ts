// src/stores/market.store.ts
import { create } from 'zustand';

interface MarketState {
  // Add market state here
}

export const useMarketStore = create<MarketState>(() => ({
  // Initial state
}));
