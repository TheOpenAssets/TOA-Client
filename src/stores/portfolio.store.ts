// src/stores/portfolio.store.ts
import { create } from 'zustand';

interface PortfolioState {
  // Add portfolio state here
}

export const usePortfolioStore = create<PortfolioState>(() => ({
  // Initial state
}));
